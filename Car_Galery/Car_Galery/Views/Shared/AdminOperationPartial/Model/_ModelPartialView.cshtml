@model List<Car_Galery.Models.ModelModel>

<div class="modal-dialog modal-lg" role="document">
    <div class="modal-content">
        <div class="modal-header">
            <h5 class="modal-title" id="exampleModalLabel"> Type Operations</h5>
            <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                <span aria-hidden="true">&times;</span>
            </button>
        </div>
        <div class="modal-body">
            <div class="card text-center">
                <div class="card-header">
                    <ul class="nav nav-tabs card-header-tabs">
                        <li class="nav-item">
                            <a id="ModelList" onclick="List(this)" class="nav-link active">Type Brand</a>
                        </li>
                        <li class="nav-item">
                            <a id="AddModel" onclick="Add(this)" class="nav-link">Add Brand</a>
                        </li>
                    </ul>
                </div>
                <div class="card-body" id="card-type-body-Model">
                    @{
                        Html.RenderPartial("_ModelListPartialView", Model);
                    }
                </div>
            </div>
        </div>
    </div>
</div>
